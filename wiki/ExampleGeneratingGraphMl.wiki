#summary Generating GraphML from an N-triples (RDF) file
#labels GraphML

= Introduction =

At the lowest level the semantic Web is built on the Resource Description Framework (RDF). RDF triples are built on a graph model.


= Details =